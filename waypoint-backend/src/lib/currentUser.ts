// Single-user/single-workspace stand-in — no real auth in this phase, matching
// the mock's behavior exactly. Must match the id seeded in db/seed.ts.
export const CURRENT_USER_ID = 'mem-1';
export const WORKSPACE_ID = 'ws-1';
