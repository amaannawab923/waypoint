/**
 * Why My Jira's counts disagree with each other and with the rail badge
 * (customer feedback round 1, Fix 9): each tab is its own Jira search.
 * On every tab count as a hover, permanently on the Connection tab.
 */
export const TAB_COUNTS_EXPLAINER =
  'Assigned, Reported, and Watching each run their own Jira search — different scopes, different counts, on purpose.';
